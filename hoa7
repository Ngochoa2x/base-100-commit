cat > base_checker.py <<'EOF'
def check_base_status(wallet, points):
    if points >= 100:
        return f"{wallet}: eligible on Base 🚀"
    return f"{wallet}: keep building on Base"

wallet = "0xYourWallet"
points = 42

print(check_base_status(wallet, points))
EOF

git add base_checker.py
git commit -m "add base checker script"
git push
